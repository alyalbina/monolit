'use client';
import { AppShell } from '@/ui/shell/AppShell';
export default function Page() { return <AppShell header={<span style={{ font: '700 14px/20px var(--am-font-ui)' }}>Research</span>}><div style={{ padding: 24, maxWidth: 760 }}><p style={{ color: 'var(--am-text-secondary)' }}>RES-03 Research Library — implementation pending (design: HF 05b FR-003/040). Route reserved; no data is invented here.</p></div></AppShell>; }
