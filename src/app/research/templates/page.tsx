'use client';
import { AppShell } from '@/ui/shell/AppShell';
export default function Page() { return <AppShell header={<h1 style={{ margin: 0, font: '700 14px/20px var(--am-font-ui)' }}>Templates</h1>}><div style={{ padding: 24, maxWidth: 760 }}><p style={{ color: 'var(--am-text-secondary)' }}>RES-04 Template Catalog — implementation pending (design: HF 05b FR-004/041).</p></div></AppShell>; }
