'use client';
import { AppShell } from '@/ui/shell/AppShell';
export default function Page() { return <AppShell header={<span style={{ font: '700 14px/20px var(--am-font-ui)' }}>Account & Preferences</span>}><div style={{ padding: 24, maxWidth: 760 }}><p style={{ color: 'var(--am-text-secondary)' }}>ACC-01 — implementation pending (design: HF 09b FR-013/050). Theme switch lives in ThemeProvider.</p></div></AppShell>; }
