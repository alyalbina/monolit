# Data boundary (DOC-05/DOC-06). Demo adapter is the default; real adapter requires documented contracts.
NEXT_PUBLIC_DATA_ADAPTER=demo   # demo | real
NEXT_PUBLIC_CAP_COMPARE=false   # UX-02: Data/ComparisonTray only with capability
NEXT_PUBLIC_CAP_FILES=false     # A-03
NEXT_PUBLIC_CAP_PAYMENTS=false  # MI-04 / Q-02 — Add funds hidden, paid Run disabled without contract
NEXT_PUBLIC_CAP_IDENTITY=false  # MI-04 / A-01
NEXT_PUBLIC_W_READY=false       # Tier 3 stays hidden
